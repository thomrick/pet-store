type Cat {
  id: String
  name: String!
  adopted: Boolean
}

type Query {
  cat(id: String!): Cat
  cats: [Cat]
}

input RegisterCatInput {
  name: String!
}

type Mutation {
  register(input: RegisterCatInput!): Cat
}